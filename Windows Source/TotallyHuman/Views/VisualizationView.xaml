<UserControl x:Class="TotallyHuman.Views.VisualizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:util="clr-namespace:TotallyHuman.Utilities"
             xmlns:oxy="http://oxyplot.org/wpf"
             Background="{StaticResource BgBrush}">

    <Grid Margin="28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Style="{StaticResource H1}"
                       Text="{Binding [viz.title], Source={x:Static util:LocProxy.Instance}}" />
            <TextBlock Style="{StaticResource Muted}" Margin="0,4,0,0"
                       Text="{Binding [viz.subtitle], Source={x:Static util:LocProxy.Instance}}" />
        </StackPanel>

        <!-- Leerhinweis -->
        <Border Grid.Row="1" x:Name="EmptyCard" Style="{StaticResource Card}" Margin="0,16,0,0"
                VerticalAlignment="Top">
            <TextBlock Style="{StaticResource Muted}" HorizontalAlignment="Center" Margin="0,30"
                       Text="{Binding [viz.empty], Source={x:Static util:LocProxy.Instance}}" />
        </Border>

        <!-- Diagramme -->
        <Grid Grid.Row="1" x:Name="ChartsHost" Margin="0,16,0,0" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="2*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource Card}" Padding="8">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Style="{StaticResource H2}" Margin="6,4,0,6"
                               Text="{Binding [viz.spectrum], Source={x:Static util:LocProxy.Instance}}" />
                    <oxy:PlotView x:Name="SpectrumPlot" Background="Transparent" />
                </DockPanel>
            </Border>

            <Border Grid.Row="1" Style="{StaticResource Card}" Padding="8" Margin="0,12,0,0">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Style="{StaticResource H2}" Margin="6,4,0,6"
                               Text="{Binding [viz.segments], Source={x:Static util:LocProxy.Instance}}" />
                    <oxy:PlotView x:Name="SegmentPlot" Background="Transparent" />
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
